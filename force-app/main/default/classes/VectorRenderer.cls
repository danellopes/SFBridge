public class VectorRenderer implements IRenderer {
    public void renderCircle(Double radius)
    {
        System.debug(String.format('Drawing a circle of radius {0}', new List<Object>{radius}));
    }
}